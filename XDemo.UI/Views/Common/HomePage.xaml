<?xml version="1.0" encoding="UTF-8"?>
<base:ViewBase
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:base="clr-namespace:XDemo.UI.Views.Base;assembly=XDemo.UI"
    xmlns:extendElement="clr-namespace:XDemo.UI.Controls.ExtendedElements"
    x:Class="XDemo.UI.Views.Common.HomePage">
    <extendElement:CarouselViewExtend
        ItemsSource="{Binding DataTest}"
        Orientation="Horizontal"
        SpacingItems="10">
        <extendElement:CarouselViewExtend.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <Grid
                        WidthRequest="300">
                        
                        <BoxView
                            
                            BackgroundColor="Blue" />
                        <Label
                            VerticalOptions="CenterAndExpand"
                            HorizontalOptions="CenterAndExpand"
                            FontSize="Large"
                            TextColor="White"
                            Text="{Binding .}" />
                    </Grid>
                    
                   <!-- <BoxView
                            WidthRequest="300"
                            BackgroundColor="Red" />-->
                </ViewCell>
            </DataTemplate>
        </extendElement:CarouselViewExtend.ItemTemplate>
    </extendElement:CarouselViewExtend>
</base:ViewBase>